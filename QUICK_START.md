# 🚀 Быстрый старт для разработчиков

## Развертывание проекта за 3 минуты

### Автоматическая установка

**Windows:**
```bash
setup.bat
```

**Linux/Mac:**
```bash
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- ✅ Создаст конфигурационные файлы из шаблонов
- ✅ Создаст необходимые директории
- ✅ Установит зависимости через Composer
- ✅ Установит права доступа (Linux/Mac)

### Ручная установка

1. **Скопируйте конфигурационные файлы:**
   ```bash
   # Windows
   copy data\config.php.example data\config.php
   copy data\config-internal.php.example data\config-internal.php
   
   # Linux/Mac
   cp data/config.php.example data/config.php
   cp data/config-internal.php.example data/config-internal.php
   ```

2. **Создайте БД:**
   ```sql
   CREATE DATABASE cucrm CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

3. **Настройте `data/config-internal.php`:**
   - Параметры подключения к БД
   - Сгенерируйте ключи: `php -r "echo bin2hex(random_bytes(16)) . PHP_EOL;"`
   - Подробнее: [SECURITY_KEYS.md](SECURITY_KEYS.md)

4. **Настройте `data/config.php`:**
   - Установите правильный `siteUrl`

5. **Установите зависимости:**
   ```bash
   composer install
   ```

6. **Очистите кэш:**
   ```bash
   php clear_cache.php
   php rebuild.php
   ```

## 📋 Что в репозитории?

✅ **Включено:**
- Исходный код приложения
- Шаблоны конфигурации (`.example`)
- Кастомизации (`/custom/`)
- Скрипты установки
- Документация

❌ **НЕ включено (в .gitignore):**
- Конфигурационные файлы с паролями
- Кэш, логи, временные файлы
- Загруженные пользовательские файлы
- Зависимости (`/vendor/`)
- База данных

## 🔐 Безопасность

⚠️ **НИКОГДА не коммитьте:**
- `data/config.php`
- `data/config-internal.php`
- Файлы с паролями и ключами
- Дампы базы данных с реальными данными

## 📚 Дополнительная информация

- [README.md](README.md) - Полная документация
- [SECURITY_KEYS.md](SECURITY_KEYS.md) - Генерация ключей безопасности
