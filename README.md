# CU CRM

EspoCRM проект для управления взаимоотношениями с клиентами.

> 💡 **Для быстрого старта смотрите:** [QUICK_START.md](QUICK_START.md)

## 🚀 Быстрый старт

### Требования
- PHP 8.0 или выше
- MySQL 8.0 или выше
- Composer
- Web-сервер (Apache/Nginx)

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <your-repository-url>
   cd cucrm
   ```

2. **Установите зависимости:**
   ```bash
   composer install
   ```
   
   > 💡 **Примечание:** `composer.json` и `composer.lock` уже включены в репозиторий для фиксации версий зависимостей.

3. **Настройте конфигурацию:**
   
   Скопируйте шаблонные файлы конфигурации:
   ```bash
   cp data/config.php.example data/config.php
   cp data/config-internal.php.example data/config-internal.php
   ```

4. **Отредактируйте конфигурационные файлы:**

   **data/config-internal.php:**
   - Настройте подключение к базе данных (host, dbname, user, password)
   - Сгенерируйте случайные ключи безопасности:
     ```php
     'cryptKey' => bin2hex(random_bytes(16)),
     'hashSecretKey' => bin2hex(random_bytes(16)),
     ```

   **data/config.php:**
   - Установите правильный `siteUrl` (например: `https://your-domain.com`)

5. **Создайте базу данных:**
   ```sql
   CREATE DATABASE your_database_name CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

6. **Импортируйте дамп БД** (если есть):
   ```bash
   mysql -u your_user -p your_database_name < database_dump.sql
   ```

7. **Настройте права доступа:**
   ```bash
   chmod -R 755 data/
   chmod -R 755 custom/
   chmod -R 755 client/custom/
   ```

8. **Очистите кэш:**
   ```bash
   php clear_cache.php
   php rebuild.php
   ```

### Запуск

Запустите встроенный PHP-сервер для разработки:
```bash
php -S localhost:8080 -t public
```

Или настройте виртуальный хост на Apache/Nginx.

## 📁 Структура проекта

- `application/` - Ядро приложения
- `client/` - Frontend файлы
- `custom/` - Кастомизации и расширения
- `data/` - Конфигурация, кэш, логи, загрузки
- `vendor/` - PHP зависимости

## 🔧 Разработка

### Генерация ключей безопасности

В PHP консоли:
```php
echo bin2hex(random_bytes(16)); // для cryptKey
echo bin2hex(random_bytes(16)); // для hashSecretKey
```

### Очистка кэша
```bash
php clear_cache.php
```

### Пересборка системы
```bash
php rebuild.php
```

## 📝 Важные замечания

- **НЕ** коммитьте файлы `data/config.php` и `data/config-internal.php` в Git
- **НЕ** коммитьте директории `data/cache/`, `data/upload/`, `data/logs/`
- Всегда используйте `.example` файлы как шаблоны

## 🔐 Безопасность

- Измените стандартные логин/пароль администратора после первого входа
- Сгенерируйте уникальные ключи безопасности
- Используйте SSL сертификат для production

## 📞 Поддержка

Для вопросов и помощи обращайтесь к команде разработки.
