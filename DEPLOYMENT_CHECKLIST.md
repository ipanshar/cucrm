# Контрольный список развертывания

## ✅ Перед коммитом в Git

Убедитесь, что следующие файлы **НЕ** добавлены в Git:

```bash
# Проверьте статус
git status

# Эти файлы НЕ должны появиться в списке:
# ❌ data/config.php
# ❌ data/config-internal.php
# ❌ data/cache/*
# ❌ data/logs/*
# ❌ data/upload/*
# ❌ vendor/*
```

## ✅ Что ДОЛЖНО быть в Git

Убедитесь, что эти файлы добавлены:

- ✅ `.gitignore`
- ✅ `data/config.php.example`
- ✅ `data/config-internal.php.example`
- ✅ `README.md`
- ✅ `QUICK_START.md`
- ✅ `SECURITY_KEYS.md`
- ✅ `setup.bat` (для Windows)
- ✅ `setup.sh` (для Linux/Mac)
- ✅ `composer.json`
- ✅ Все файлы в `/application/`
- ✅ Все файлы в `/client/`
- ✅ Все файлы в `/custom/` (ваши кастомизации)

## 🔍 Проверка перед первым push

```bash
# 1. Убедитесь, что .gitignore работает
git status

# 2. Проверьте, что шаблоны добавлены
git add data/config.php.example
git add data/config-internal.php.example

# 3. Проверьте список файлов для коммита
git diff --cached --name-only

# 4. Если все ОК, коммитьте
git commit -m "Initial commit: CU CRM project"

# 5. Добавьте remote и push
git remote add origin <your-github-url>
git branch -M main
git push -u origin main
```

## 🎯 Для нового разработчика

После клонирования репозитория:

```bash
# 1. Клонировать
git clone <repository-url>
cd cucrm

# 2. Запустить установку (Windows)
setup.bat

# ИЛИ для Linux/Mac
chmod +x setup.sh
./setup.sh

# 3. Отредактировать конфигурацию
# - data/config-internal.php (БД + ключи)
# - data/config.php (siteUrl)

# 4. Создать БД и импортировать данные

# 5. Готово!
php clear_cache.php
php rebuild.php
```

## 📝 Дополнительные команды

```bash
# Просмотр игнорируемых файлов
git status --ignored

# Проверка, что файл игнорируется
git check-ignore -v data/config.php

# Удалить файл из Git (если случайно добавили)
git rm --cached data/config.php
git commit -m "Remove sensitive config file"
```

## ⚠️ Частые ошибки

### Ошибка: "Случайно добавил config.php в Git"

```bash
# Удалите из Git, но оставьте локально
git rm --cached data/config.php
git rm --cached data/config-internal.php

# Добавьте в .gitignore (если еще не добавлено)
# Коммитьте изменения
git commit -m "Remove sensitive files from Git"
git push
```

### Ошибка: "У коллеги не работает после клонирования"

Проверьте:
1. ✅ Создали ли конфигурационные файлы?
2. ✅ Настроили ли подключение к БД?
3. ✅ Установили ли зависимости (`composer install`)?
4. ✅ Создали ли БД и импортировали дамп?
5. ✅ Запустили ли `php clear_cache.php` и `php rebuild.php`?
