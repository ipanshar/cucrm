# Пример файла переменных окружения для CU CRM
# Скопируйте этот файл в .env и настройте под свое окружение
# ВАЖНО: Файл .env не должен попадать в Git!

# База данных
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# Ключи безопасности (сгенерируйте новые!)
# Используйте: php -r "echo bin2hex(random_bytes(16)) . PHP_EOL;"
CRYPT_KEY=GENERATE_RANDOM_32_CHAR_STRING
HASH_SECRET_KEY=GENERATE_RANDOM_32_CHAR_STRING

# URL приложения
SITE_URL=http://localhost:8080

# SMTP (опционально)
SMTP_SERVER=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_SECURITY=TLS
SMTP_FROM_NAME=CU CRM
SMTP_FROM_ADDRESS=

# Окружение
APP_ENV=development
APP_DEBUG=true

# Логирование
LOG_LEVEL=WARNING
